import { TelaEscala } from './TelaEscala';

/**
 * Escala do Time.
 *
 * Mesmo padrão de dois modos da Ordem do Culto: coordenador monta na
 * semana, qualquer pessoa logada confirma presença. Ver `TelaEscala` para
 * a decisão de modo.
 */
export default function PaginaEscala() {
  return <TelaEscala />;
}
